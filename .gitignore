# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/
env/

# Scored output is DERIVED from raw_outputs*.jsonl - regenerate with scoring.py
# rather than committing it, so it never causes merge conflicts.
results/scored_outputs.jsonl

# Plots are regenerated by analysis.py
results/plots/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Model caches (never commit - these are gigabytes)
*.safetensors
*.bin
.cache/
